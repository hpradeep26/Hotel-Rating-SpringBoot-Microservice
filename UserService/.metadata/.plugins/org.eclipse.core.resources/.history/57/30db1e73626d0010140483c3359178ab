server:
  port: 8064
  
  address: 127.0.0.1

spring:
  cloud:
    gateway:
      routes:
        - id: USER-SERVICE
          uri: lb://USER-SERVICE
          predicates:
            - "Path=/users/**"
    
